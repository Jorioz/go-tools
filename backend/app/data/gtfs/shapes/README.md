## data/gtfs/shapes/

Data here has been gitignored. This file holds the shapefiles for each route, which can be found in raw/GO-GTFS/shapes.txt.
A helper function in /utils is used to split the raw files into individual ones.

todo: on init, if these files are missing, the helper function will run.
For now, call manually.
